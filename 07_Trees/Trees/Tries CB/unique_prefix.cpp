// Given an array of words, find the *smallest* words which are prefix to only 1 word
// input[] = ["car","cobra","dove","duck","c", "dog"]
// output[] = ["ca", "co", "dog", "dov", "du"], duck is also unique but the word duc is smaller and unique, but du is even smaller and still unique


#include <bits/stdc++.h>
using namespace std;

int main (){

    return 0;
}